# Personal Configuration

This directory is an independent Git repository for non-secret personal Agent rules, notes, and helper scripts.

- Remote branch: `study734/WorkTaskFlow:personal-config`
- Never merge this branch into `member1`.
- Never store secrets, credentials, personal data, `.env` files, or confidential team material here.
- The outer `WorkTaskFlow-member1` repository ignores this entire directory.
