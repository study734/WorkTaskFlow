limit_req_zone $binary_remote_addr zone=auth_limit:10m rate=10r/m;

server {
    listen 80;
    server_name ${DOMAIN_NAME} www.${DOMAIN_NAME};

    set $forwarded_proto http;
    set $forwarded_port 80;
    set $hsts_header "";

    include /etc/nginx/snippets/app-locations.conf;
}
